services:
  client-1:
    build: .
    volumes:
      - ../../data/client_1/data.csv:/app/data.csv
    networks:
      - fl-network

  client-2:
    build: .
    volumes:
      - ../../data/client_2/data.csv:/app/data.csv
    networks:
      - fl-network

networks:
  fl-network:
    driver: bridge
